FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p /app/traces

# Run ARQ worker
CMD ["arq", "app.worker.tasks.WorkerSettings"]
